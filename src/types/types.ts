export type Student = {
    name: string;
    grade: number;
};

export type Movie = {
    title: string;
    genre: string;
    showTime: number;
};

export type Competitor = {
    name: string;
    score: number;
};
